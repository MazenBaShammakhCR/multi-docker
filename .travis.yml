sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mazenbashammakhcr/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true mazenbashammakhcr/react-test npm test

after_success:
  - docker build -t mazenbashammakhcr/multi-client ./client
  - docker build -t mazenbashammakhcr/multi-nginx ./nginx
  - docker build -t mazenbashammakhcr/multi-server ./server
  - docker build -t mazenbashammakhcr/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push mazenbashammakhcr/multi-client
  - docker push mazenbashammakhcr/multi-nginx
  - docker push mazenbashammakhcr/multi-server
  - docker push mazenbashammakhcr/multi-worker
